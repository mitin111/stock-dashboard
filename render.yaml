services:
  - type: worker        # <-- Worker = No public port
    name: backend-stream
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend_stream_server.py   # ✅ No uvicorn
    plan: standard

  - type: web           # <-- This is the UI
    name: stock-dashboard
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run stock_dashboard_phase1.py --server.address=0.0.0.0 --server.port=$PORT --server.headless=true






